{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.tu-dien-tieng-viet/schema/reference-non-vi.schema.json",
  "title": "ReferenceNonViEntry",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "reference_id",
    "cluster_id",
    "word",
    "definition_count",
    "languages",
    "sources",
    "reasons",
    "canonical_definition",
    "source_definitions"
  ],
  "properties": {
    "reference_id": { "type": "string", "pattern": "^reference-" },
    "cluster_id": { "type": "string", "pattern": "^cluster-" },
    "word": { "type": "string", "minLength": 1 },
    "definition_count": { "type": "integer", "minimum": 1 },
    "languages": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "reasons": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true
    },
    "canonical_definition": { "$ref": "#/$defs/sourceDefinition" },
    "source_definitions": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/sourceDefinition" }
    }
  },
  "$defs": {
    "sourceDefinition": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "sense_id",
        "definition_uid",
        "entry_id",
        "source",
        "source_entry_id",
        "raw_record_hash",
        "language",
        "review_status",
        "definition_index",
        "confidence",
        "risk_tags",
        "meaning"
      ],
      "properties": {
        "sense_id": { "type": "string", "pattern": "^sense-" },
        "definition_uid": { "type": "string", "pattern": "^def-" },
        "entry_id": { "type": "string", "minLength": 1 },
        "source": { "type": "string", "minLength": 1 },
        "source_entry_id": { "type": ["string", "null"] },
        "raw_record_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "language": { "type": "string", "minLength": 1 },
        "review_status": {
          "type": "string",
          "enum": ["unreviewed", "machine-checked", "human-reviewed"]
        },
        "definition_index": { "type": "integer", "minimum": 0 },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "risk_tags": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        },
        "meaning": { "type": "string", "minLength": 1 }
      }
    }
  }
}
